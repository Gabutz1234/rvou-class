{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "Personal portfolio website built with HTML5, Tailwind CSS, and Vanilla JavaScript",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "fast-check": "^3.19.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.1.0"
  }
}
